#include<iostream>
using  namespace std;

int main() {
	int n;
	cin >> n;
	int start = 1;
	int count = 0;

	while (start <= n) {
		// int a, b, c;
		// cin >> a >> b >> c;
		// int sum = a + b + c;

		// if (sum >= 2) {
		// 	count = count + 1;
		// }

		start = start + 1;


		cout << count << endl;
	}




